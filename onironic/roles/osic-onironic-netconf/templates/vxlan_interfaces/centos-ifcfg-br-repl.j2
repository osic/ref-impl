DEVICE=br-repl
TYPE=Bridge
IPADDR={{ repl_network.split('.')[0:2] | join('.') }}.{{ repl_network.split('.')[2]|int + third_oct_diff|int }}.{{ ansible_ssh_host.split('.')[-1] }}
NETMASK={{ repl_netmask }}
ONBOOT=yes
NM_CONTROLLED=no
DELAY=0
